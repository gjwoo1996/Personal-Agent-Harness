---
description: Personal-Agent-Harness development (adding rule domains, bin/pah)
globs: bin/**,standards/**,templates/**,docs/**,dev-docs/**,tests/**
alwaysApply: false
harness-internal: true
---

# Personal-Agent-Harness Development

When modifying **Personal-Agent-Harness itself** (not applying the harness to other projects), first read:

- `dev-docs/internal/adding-rule-domains.md` — adding new rule domains
- `dev-docs/internal/development.md` — harness development and testing

Follow those documents for harness engineering work.

Do not confuse this with target-project work:

- Applying standards to other projects uses `setup.sh` / `update.sh` and devcontainer rules.
- Harness development docs under `dev-docs/` are **not** copied to target projects by `pah install`.

After changes to `bin/pah`, stubs, or standards, run:

```bash
bash tests/test_pah.sh
```
