# 03. 진행 완료 작업

브랜치: `feat/external-harness-workflow`  
기준일: 2026-06-09

---

## A. 커밋 완료 (5 commits)

| 커밋 | 내용 |
|------|------|
| `5c7eb2a` | 설계 스펙: `docs/superpowers/specs/2026-06-09-external-harness-workflow-design.md` |
| `5b9ce06` | `bootstrap.sh` 추가 (외부 harness에서 init, `--clean-nested`) |
| `db94645` | `gitignore` managed block에 `Personal-Agent-Harness/` 추가 |
| `62d0332` | `VERSION` → `0.3.0` |
| `f489b05` | 문서 개편 (PAH_HOME 워크플로우), 구현 계획 문서 |

### A-1. `bootstrap.sh`

- `setup.sh` 실행 후 안내 메시지
- `--clean-nested`: 프로젝트 안 `Personal-Agent-Harness/` 제거
- 중첩 clone 경고

### A-2. `bin/pah` 확장 (커밋분)

- `pah status --harness-root`: 설치 버전 vs harness `VERSION` 비교
- `harness_version`, `update available` 출력
- `gitignore` 컴포넌트: `Personal-Agent-Harness/` 무시

### A-3. 테스트 (커밋분)

- 외부 bootstrap / clean-nested / external update 시나리오
- status 버전 비교
- `grep -q` 파이프 SIGPIPE → 파일 기반 assertion으로 수정

### A-4. 문서 (커밋분)

- `README.md`, `docs/usage.md`, `docs/how-it-works.md`, `docs/reference.md`, `docs/troubleshooting.md` — PAH_HOME 권장

---

## B. 작업 완료 · 미커밋 (working tree)

다음은 구현·테스트 완료했으나 **아직 git commit 하지 않음**.

| 파일 | 변경 |
|------|------|
| `package.json` | **신규** — `personal-agent-harness@0.3.0`, npm `files` 화이트리스트 |
| `bin/pah-entry` | **신규** — npm bin, `PAH_DISTRIBUTION=npm` |
| `bin/pah` | `init`, `update` 서브커맨드; status 기본 CLI 버전 비교 |
| `update.sh` | `.git` 없으면 `git pull` 스킵 |
| `bootstrap.sh` | `pah init` 위임 |
| `tests/test_pah.sh` | `init`/`update`/npm 패키지 시뮬레이션 테스트 |
| `README.md` | npx 1순위 |
| `docs/usage.md` | npx 워크플로우 |
| `docs/reference.md` | `init`/`update` 레퍼런스 |
| `docs/development.md` | npm publish 가이드 |
| `.gitignore` | **신규** — `secret/` 제외 |

### B-1. npm 패키지 구조

`package.json` `files`에 포함:

```text
bin/pah, bin/pah-entry
bootstrap.sh, setup.sh, update.sh, install.sh
VERSION, config/, standards/, templates/
```

**제외:** `docs/`, `tests/`, `.cursor/`, `AGENTS.md` (하네스 개발용)

### B-2. CLI 명령

| 명령 | 동작 |
|------|------|
| `pah init <target>` | setup + verify, optional `--clean-nested` |
| `pah update <target>` | install + verify (npm: git pull 없음) |
| `pah status <target>` | manifest vs CLI `VERSION` 비교 |

### B-3. 테스트

```bash
bash tests/test_pah.sh
# → All pah tests passed ✅
```

---

## C. 생성된 정리·계획 문서

| 경로 | 용도 |
|------|------|
| `docs/superpowers/plans/2026-06-09-external-harness-workflow.md` | Approach C 구현 계획 |
| `docs/superpowers/specs/2026-06-09-external-harness-workflow-design.md` | 설계 스펙 |
| `etc/` (본 폴더) | 요청·진행·잔여 작업 종합 정리 |

---

## D. 의도적으로 하지 않은 것 (YAGNI)

- `curl | bash` 원격 installer
- `pah status --check-remote` (git fetch)
- submodule / symlink mode
- npm private 패키지 (유료 Pro)
- GitHub Actions 자동 publish (아직)
- Windows cmd 네이티브 지원
